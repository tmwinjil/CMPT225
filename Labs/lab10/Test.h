#ifndef TEST_H
#define TEST_H

using namespace std;

class Test {
public:
void test(int n, int max);	// creates a new array of n random integers in the range [0, max)
                           // then runs unique on this array and prints the array and unique's answer.
private:
};
#endif